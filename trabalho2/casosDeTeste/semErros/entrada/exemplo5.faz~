constante tempo: inteiro = 1000
constante pinoLCD: inteiro = 21

declare
	y: real
declare
	mensagem: literal

comando_setup
	
	ativar(lcd, pinoLCD)

fim_comando_setup

comando_loop	
	definirCor(lcd, pinoLCD, (10, 200, 200))
	escrever(lcd, pinoLCD, ":D  ")
	esperar(tempo)
fim_comando_loop
